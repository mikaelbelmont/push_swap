#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

#include "libft/libft.h"
#include "stdio.h"

void	ft_checker(t_list **a, char **av);
void	sa(t_list **a);
void	sort2(t_list **a);
void	ft_end(t_list **stack);

#endif 